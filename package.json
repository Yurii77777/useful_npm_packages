{
  "name": "useful_npm_packages",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "start": "node ./dist/index.js",
    "dev": "nodemon -e ts --exec \"npm run build && npm start\"",
    "compile": "npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "randomatic": "^3.1.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "ts-loader": "^9.2.6",
    "ts-node": "^9.1.1",
    "typescript": "^5.0.4"
  },
  "prettier": "./.prettierrc.json"
}
